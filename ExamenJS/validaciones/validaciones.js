function validar(campo) {
    if (campo.value == null || campo.value.length == 0) {        
        mostrarError(campo.id, "Error: No contiene @");    
    var str = "Email";
    var n = str.indexOf("email");
     n = @ }
}